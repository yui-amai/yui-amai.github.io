---
layout: '@/layouts/Category.astro'
title: アーカイブ
description: 日付別に記事を閲覧
---

import { archives } from '@/config/blog-jp';

<div className="relative mt-8 space-y-6">
  {archives.map((yearData) => (
    <div className="relative">
      <div className="absolute left-0 top-0 w-3 h-3 bg-black/20 dark:bg-white/20 rounded-full border-2 border-white dark:border-black"></div>
      <div className="ml-6">
        <h2 className="text-xl font-medium text-black/80 dark:text-white/80 mb-3">{yearData.year}</h2>
        <div className="space-y-2">
          {yearData.months.map((monthData) => (
            <a href="#" className="group block py-2 hover:bg-black/5 dark:hover:bg-white/5 transition-all duration-200 rounded px-2">
              <div className="flex items-center justify-between">
                <div>
                  <span className="text-sm text-black/60 dark:text-white/60">{monthData.month} {yearData.year}</span>
                  <span className="text-xs text-black/40 dark:text-white/40 ml-2">{monthData.count} 記事</span>
                </div>
                <div className="text-black/40 dark:text-white/40 group-hover:text-black/60 dark:group-hover:text-white/60 transition-colors text-sm">
                  →
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>
    </div>
  ))}

  {/* Timeline line */}
  <div className="absolute left-1.5 top-0 bottom-0 w-0.5 bg-black/20 dark:bg-white/20"></div>
</div>
