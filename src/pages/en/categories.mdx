---
layout: '@/layouts/Category.astro'
title: Categories
description: Browse posts by category
---

import { categories } from '@/config/blog';

<div className="mt-8 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  {categories.map((category) => (
    <a href={`/en/categories/${category.name.toLowerCase().replace(/\s+/g, '-')}`} className="block h-72">
      <div className="bg-white/10 dark:bg-white/10 rounded-lg p-6 backdrop-blur-sm border border-white/30 dark:border-white/10 hover:bg-black/5 dark:hover:bg-white/20 transition-all duration-200 shadow-sm hover:shadow-md group cursor-pointer h-full flex flex-col">
        <div className="text-center flex-grow flex flex-col justify-between h-full">
          <div>
            <div className="text-3xl mb-4 text-[#8e7f9d] dark:text-[#6b8cc7] group-hover:text-[#6d5d7a] dark:group-hover:text-[#8ca8e8] transition-colors flex items-center justify-center">
              <i data-lucide={category.icon}></i>
            </div>
            <h3 className="text-xl font-semibold text-black/90 dark:text-white/90 mb-3 group-hover:text-black dark:group-hover:text-white transition-colors">
              {category.name}
            </h3>
            <p className="text-black/70 dark:text-white/70 mb-4 leading-relaxed text-sm overflow-hidden" style="display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical; text-overflow: ellipsis;">
              {category.description}
            </p>
          </div>
          <div className="text-sm text-black/60 dark:text-white/60">
            {category.count} posts
          </div>
        </div>
      </div>
    </a>
  ))}
</div>
