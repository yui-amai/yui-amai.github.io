---
layout: '@/layouts/Category.astro'
title: Tags
description: Browse posts by tags
---

import { tags } from '@/config/blog';

<div className="mt-8">
  <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    {tags.map((tag) => (
      <a href={`/en/tags/${tag.name.toLowerCase()}`} className="group block p-4 bg-white/10 dark:bg-white/10 rounded-lg backdrop-blur-sm border border-white/30 dark:border-white/10 hover:bg-black/5 dark:hover:bg-white/20 transition-all duration-200 shadow-sm hover:shadow-md no-underline">
        <div className="flex items-center justify-between">
          <span className="font-medium text-black/80 dark:text-white/80 group-hover:text-black dark:group-hover:text-white transition-colors no-underline">#{tag.name}</span>
          <span className="text-sm text-black/60 dark:text-white/60">{tag.count}</span>
        </div>
      </a>
    ))}
  </div>
</div>
